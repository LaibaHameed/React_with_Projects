import React from "react";
const UserContext = React.createContext();
// import { createContext } from "react"; works exactly same
// export const UserContext = createContext(0);


export default UserContext;